打印("=== 第四课：条件判断 ===")
打印("")

分数 = 85

如果 (分数 >= 90) {
    打印("优秀！")
} 否则 如果 (分数 >= 80) {
    打印("良好！")
} 否则 如果 (分数 >= 60) {
    打印("及格")
} 否则 {
    打印("需要努力")
}
打印("")

// 判断奇偶
n = 7
如果 (n % 2 == 0) {
    打印(n + " 是偶数")
} 否则 {
    打印(n + " 是奇数")
}
打印("")

// 布尔运算
x = 10
y = 20
如果 (x > 5 && y < 30) {
    打印("x > 5 且 y < 30: 成立！")
}
