// 简单基准测试（无递归）
打印("===== 循环性能 =====");
变量 sum = 0;
变量 i = 0;
循环 (i = 0; i < 1000000; i = i + 1) {
    sum = sum + i;
}
打印("sum(0..999999) = ", sum);

打印("===== 数组性能 =====");
变量 arr = [];
变量 j = 0;
循环 (j = 0; j < 10000; j = j + 1) {
    追加(arr, j);
}
打印("arr len = ", len(arr));
打印("完成");
