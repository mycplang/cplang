枚举 结果 {
    成功(字符串)
    | 失败(字符串)
};

函数 主() {
    设 x 为 make_结果_成功("操作完成");
    匹配 x {
        情况 成功(消息) => {
            打印("成功: " + 消息);
        }
        | 情况 失败(错误) => {
            打印("失败: " + 错误);
        }
    }
}
