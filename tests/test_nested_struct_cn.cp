// 嵌套结构体测试 - 中文版本

结构体 点 {
    x;
    y;
};

结构体 矩形 {
    左上角;
    右下角;
};

函数 主() {
    // 嵌套结构体字面量
    变量 r = 矩形{
        左上角: 点{x: 0, y: 0},
        右下角: 点{x: 100, y: 100}
    };
    
    // 访问嵌套字段
    打印(r.左上角.x);
    打印(r.左上角.y);
    打印(r.右下角.x);
    打印(r.右下角.y);
    
    打印("嵌套结构体测试完成");
}
