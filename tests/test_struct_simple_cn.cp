// 测试结构体类型系统 - 简化版 - 中文版本

结构体 点 {
    x;
    y;
};

函数 主() {
    // 创建结构体实例（使用Table模拟）
    变量 p = {};
    p.x = 10;
    p.y = 20;
    
    // 访问成员
    打印(p.x);
    打印(p.y);
    
    // 修改成员
    p.x = 100;
    p.y = 200;
    
    打印(p.x);
    打印(p.y);
    
    打印("结构体测试完成");
}
