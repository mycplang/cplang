// 泛型结构体约束测试
// 带约束的泛型结构体
结构体 盒子<T: 可字符串化> {
    T 内容;
}

// 泛型函数使用带约束的结构体
函数 打印盒子<T: 可字符串化>(b: 盒子<T>) {
    打印("盒子内容: ", b.内容);
}

函数 main() {
    变量 intBox = 盒子<整数>{内容: 42};
    变量 strBox = 盒子<字符串>{内容: "hello"};
    
    打印盒子(intBox);
    打印盒子(strBox);
    
    返回 0;
}
