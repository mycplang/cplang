// 泛型约束 - 违反约束测试
// 尝试用不满足约束的类型调用泛型函数
函数 相加<T: 可相加>(a: T, b: T): T {
    返回 a + b;
}

函数 main() {
    // 整数满足"可相加"，应该通过
    变量 x = 相加<整数>(10, 20);
    打印("相加(10, 20) = ", x);
    
    // 字符串满足"可相加"，应该通过
    变量 y = 相加<字符串>("hello", " world");
    打印("相加('hello', ' world') = ", y);
    
    返回 0;
}
