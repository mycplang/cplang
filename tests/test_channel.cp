函数 main() {
    变量 ch = 通道创建(1);
    变量 ok = 通道发送(ch, 42);
    打印("ok = ");
    打印(ok);
    如果 (非 ok) { 返回 1; }
    变量 r = 通道尝试接收(ch);
    打印("r[0] = ");
    打印(r[0]);
    打印("r[1] = ");
    打印(r[1]);
    返回 0;
}
