// 测试结构体类型系统 - 简化版 - 中文版本

结构体 点 {
    x;
    y;
};

函数 主() {
    // 使用数组作为结构体替代
    变量 p = [];
    p[0] = 10;  // x
    p[1] = 20;  // y
    
    打印(p[0]);
    打印(p[1]);
    
    // 修改
    p[0] = 100;
    p[1] = 200;
    
    打印(p[0]);
    打印(p[1]);
    
    打印("数组模拟结构体测试完成");
}
