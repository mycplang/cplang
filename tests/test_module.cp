// 模块系统测试

// 主模块测试
导入 math_utils;

变量 x = 10;
变量 y = 20;

// 使用导入的模块函数
变量 sum = 加法(x, y);
变量 product = 乘法(x, y);

打印("x = ");
打印(x);
打印(", y = ");
打印(y);
打印("\n");

打印("sum = ");
打印(sum);
打印("\n");

打印("product = ");
打印(product);
打印("\n");

// 测试循环依赖检测
// 导入 "circular_a";  // 这应该会检测到循环依赖
