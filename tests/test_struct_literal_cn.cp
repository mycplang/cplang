// 结构体字面量测试 - 中文版本

结构体 点 {
    x;
    y;
};

函数 主() {
    // 使用结构体字面量创建实例
    变量 p = 点{x: 10, y: 20};
    
    // 访问字段
    打印(p.x);
    打印(p.y);
    
    // 创建另一个实例
    变量 p2 = 点{x: 100, y: 200};
    打印(p2.x);
    打印(p2.y);
    
    打印("结构体字面量测试完成");
}
