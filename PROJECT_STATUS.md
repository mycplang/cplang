# CP 语言编译器/虚拟机 - 当前状态

**项目路径**: `C:\Users\Administrator\Desktop\cplang\`

## ✅ 已完成功能

### 核心编译管线
- ✅ Lexer（词法分析器）- 支持 UTF-8 中文关键字
- ✅ Parser（语法分析器）- 生成 AST
- ✅ Semantic Analyzer（语义分析器）
- ✅ Codegen（代码生成器）- 16字节指令格式
- ✅ VM（虚拟机）- 栈式虚拟机，支持 GC

### 数据类型
- ✅ 整数（Int）
- ✅ 浮点数（Float）
- ✅ 布尔值（Bool）
- ✅ 字符串（String）- UTF-8 支持
- ✅ 数组（Array）
- ✅ 表（Table/Map）
- ✅ Nil

### 运算符
- ✅ 算术运算：`+`, `-`, `*`, `/`, `%`
- ✅ 比较运算：`==`, `!=`, `<`, `>`, `<=`, `>=`
- ✅ 逻辑运算：`&&`, `||`, `!`
- ✅ 位运算：`&`, `|`, `^`, `~`, `<<`, `>>`

### 控制流
- ✅ if/else（`如果/否则`）
- ✅ while 循环（`当`）
- ✅ for 循环（`循环`）
- ✅ for-each 循环（`遍历`）
- ✅ return（`返回`）

### 函数
- ✅ 函数声明（`函数`）
- ✅ 函数调用
- ✅ 递归函数
- ✅ 多参数函数
- ✅ 内置函数（`打印`）

### 数组操作
- ✅ 数组字面量 `[1, 2, 3]`
- ✅ 数组索引访问 `arr[0]`
- ✅ 数组元素赋值 `arr[0] = 10`
- ✅ for-each 遍历数组

### 中文支持
- ✅ 中文关键字：`变量`, `常量`, `函数`, `返回`, `如果`, `否则`, `当`, `循环`, `遍历`, `打印`, `真`, `假`, `空`
- ✅ 中文变量名
- ✅ 中文函数名
- ✅ UTF-8 字符串字面量

## 🔧 架构特性

### 字节码格式
- 16字节对齐指令
- 格式：`[op][0][a][b][c][imm32][padding4]`
- 支持 `emit`, `emitInt`, `emitJump` 三种指令生成方式

### GC（垃圾回收）
- 三色标记算法
- 自动内存管理
- 支持循环引用检测

## 📊 测试验证

### 基础测试
```cplang
变量 x = 10 + 20;
打印(x);  // 输出: 30
```

### 函数测试
```cplang
函数 add(a, b) {
    返回 a + b;
}
打印(add(2, 3));  // 输出: 5
```

### 递归测试
```cplang
函数 fibonacci(n) {
    如果 (n <= 1) { 返回 n; }
    返回 fibonacci(n - 1) + fibonacci(n - 2);
}
打印(fibonacci(10));  // 输出: 55
```

### 数组测试
```cplang
变量 arr = [10, 20];
遍历 (x : arr) {
    打印(x);  // 输出: 10, 20
}
```

### 字符串测试
```cplang
变量 s = "Hello, 世界!";
打印(s);  // 输出: Hello, 世界!
```

## 🚀 企业级路线图

1. ✅ 用户自定义函数
2. ✅ 数组
3. ✅ 字符串字面量
4. ⏳ 基础标准库（进行中）
5. ⏳ 模块系统

## 🐛 已知问题

1. ⚠️ printVal 不处理 Bool 类型（输出 `<1>` 而非 `true`）
2. ⚠️ 解析器只支持中文关键字，无 var/let 英文别名
3. ⚠️ 代码中仍有临时调试输出待清理

## 📝 最近修复

### 2026-04-17: ForEach 循环无限循环修复
- **问题**：`遍历 (x : arr) { ... }` 产生无限循环
- **根因**：`emit(OP_LOADINT, ...)` 应使用 `emitInt(OP_LOADINT, ...)`
- **修复**：确保立即数在正确位置（字节4-7而非字节3）

### 2026-05-22: JIT 运行时符号扩展 + 基准测试加速
- **新增 `jit_tick` 运行时函数**：为 `tick()`/`tock()` 创建 C-linkage JIT 运行时包装，注册为 ORC JIT 符号
- **LLVM 代码生成适配**：`tick`/`tock` 调用映射为 `jit_tick`，类似 `打印`→`jit_printv`
- **Native 调用移除白名单**：`jit_*` 前缀的已知 JIT 运行时函数不再触发 native call 移除
- **基准测试改造**：
  - `test_loop100m.cp`：100M 循环包裹为 `loopSum()` 函数，支持 JIT 全量编译加速
  - `jit_bench_loop.cp`：循环逻辑提取为独立函数，热点 JIT 可单独编译计算密集型部分
- **效果**：使用 `--jit` 全量 JIT 模式或 `--hotspot` 热点模式均可显著加速基准测试

---

### 2026-05-22: 错误信息全汉化 + WindowsApps Python 存根修复
- **语义错误 Line 0 修复**：`defineSymbol` 重复定义错误从 AST 节点提取行号
- **解析器全汉化**：16处英文错误消息全部汉化（"Expected package name" → "需要包名"等）
- **错误格式标准化**：`"Line X, Col Y"` → `"第X行第Y列"`，`"L行号"` → `"第X行"`
- **编译器前缀汉化**：`"Parser:"` → `"语法分析:"`，`"Semantic:"` → `"语义分析:"`，`"Codegen:"` → `"代码生成:"`
- **VM 错误汉化**：`"unknown opcode"` → `"未知操作码"`
- **WindowsApps Python 存根修复**：`python3` 原指向 0 字节存根，已创建 `python3.bat` 包装器转发到真实 Python（Python 3.13.13）
- **验证**：`python --version` / `python3 --version` / `pip --version` / `py --version` 均正常工作

---

**最后更新**: 2026-05-22 21:00 GMT+8
